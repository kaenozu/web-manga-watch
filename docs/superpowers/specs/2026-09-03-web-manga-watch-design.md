# 仕様書：WEBマンガウォッチ

## 1. 目的

「WEBマンガウォッチ」は、複数の公式WEB漫画サイトに掲載されている作品の新話公開をひとまとめに確認できる一般公開Webサービスである。

副次的に、作品名・作者名・掲載サイト名で作品を検索し、新しい作品を発見できるようにする。漫画本文や画像は保存・配信せず、各公式サイトへ誘導する。将来的には通知機能を追加し、更新の見逃し防止へ拡張する。

### MVP対象作品

- 『ふたりバス』— サンデーうぇぶり
- 『妹は知っている』— ヤンマガWeb

### 成功条件

- 対象2作品の新話を重複なく検出できる
- 利用者が登録なしでフォローと既読管理を始められる
- 公式ページへの導線を提供できる
- 単行本のKindle版・紙版へのAmazonリンクを表示できる

## 2. MVPの範囲

### 含める機能

- 未ログインでの作品・作者・サイト検索と閲覧
- Cookieによる匿名利用者管理
- 漫画・作者・掲載サイトのフォロー
- フォロー対象の更新一覧
- 更新話の自動既読化と手動での未読戻し
- 作品名・作者名・サイト名による検索
- 作品詳細でのKindle版・紙版Amazonリンク
- 1日1回の更新取得
- RSS優先、RSSがない場合のHTML解析

### MVPに含めない機能

- 会員登録・ログイン
- メール通知・Webプッシュ通知
- ランキング、レビュー、コメント
- 運営者向け管理画面
- ユーザーによる任意サイト追加
- 漫画本文・画像の保存または配信

## 3. 技術構成

- フロントエンド：Next.js 16 + vinext
- ホスティング・実行基盤：Cloudflare Workers
- データベース：Supabase PostgreSQL
- 定期処理：Supabase CronからEdge Functions等をスケジュール実行
- 利用者識別：個人情報を含まないランダムな匿名ID Cookie
- 開始時のプラン：Supabase Freeを基本とする

利用者増加だけを理由に有料化せず、容量、通信量、バックアップ、安定運用の必要性を見てProへ移行する。

## 4. データモデル

### Site

- name
- base_url
- source_type（rss または html）
- source_url
- enabled
- last_fetched_at
- last_success_at
- last_fetch_status
- last_error
- last_episode_count
- consecutive_failures

### Author

- id
- name
- normalized_name
- created_at

### Series

- id
- title
- site_id
- series_url
- description
- thumbnail_url
- update_day
- kindle_asin
- paperback_asin

### SeriesAuthor

- series_id
- author_id
- role

### Episode

- series_id
- site_id
- source_episode_key
- title
- episode_number（取得できる場合）
- published_at
- official_url
- first_detected_at
- is_promotional

`source_episode_key` はサイトアダプターが生成する取得元固有の安定キーとし、`UNIQUE(site_id, source_episode_key)`で重複を防ぐ。URLは変更される可能性があるため、同一話の識別子そのものにはしない。

### AnonymousUser

- anonymous_id_hash
- created_at
- last_seen_at

### SeriesFollow

- anonymous_user_id
- series_id
- created_at

### AuthorFollow

- anonymous_user_id
- author_id
- created_at

### SiteFollow

- anonymous_user_id
- site_id
- created_at

フォロー対象ごとにテーブルを分け、外部キーで参照整合性を保証する。

### ReadState

- anonymous_user_id
- episode_id
- is_read
- updated_at

## 5. 更新取得仕様

1. 1日1回、サイトごとの取得処理を実行する。
2. 対応サイトのRSSが利用可能ならRSSを優先する。
3. RSSがない場合は作品ページ等をHTML解析する。
4. 外部IDまたは正規化済み公式URLで既存話と照合する。
5. 未登録の話だけを新規登録する。
6. 公開日時の降順で更新一覧に表示する。
7. PR、単行本告知、キャンペーン、投票、お知らせは本編更新から除外する。
8. 公開終了した話は履歴から削除しない。
9. 取得失敗時は既存データを変更せず、サイト単位でエラーを記録する。
10. HTTP 200でも前回の話数から不自然に0件になった場合は異常と判定し、成功扱いにしない。
11. サイトへのアクセスは低頻度とし、利用規約・robots.txt・アクセス制限を尊重する。

## 6. 利用者体験

- 初回アクセス時に匿名ID Cookieを発行する。
- Cookieにはフォロー情報や個人情報を直接保存しない。
- 更新話への公式リンクをクリックした時点で既読にする。
- 既読話には手動で未読へ戻す操作を用意する。
- Cookie削除、別ブラウザ、別端末では状態を引き継がない。
- 180日間アクセスがない匿名利用者のフォロー・既読データは削除対象とする。
- 将来ログインを追加する場合は、匿名データをアカウントへ移行できる構造にする。
- 既読化リクエストは`navigator.sendBeacon`または`fetch(..., { keepalive: true })`で送信し、公式ページへの遷移でリクエストが失われにくくする。

## 7. Amazonアフィリエイト

作品詳細ページに、作品ごとに手動登録したAmazon商品情報を表示する。

- Kindle版へのリンク
- 紙の単行本へのリンク
- 必要に応じて既刊・最新巻へのリンク

初期はASINを開発者が登録する。価格や在庫を自サイトに固定表示せず、Amazonの商品ページへ誘導する。アソシエイト承認後にタグ付きリンクを有効化し、サイト内にAmazonアソシエイトである旨を明示する。承認前は通常リンクまたはリンク非表示とする。

Amazonの利用規約・プログラムポリシーに従い、商品画像・価格・商品情報の利用方法は実装前に最新条件を確認する。

## 8. 画面構成

- トップページ：サービス説明、最新更新、検索、作品発見
- 更新一覧：フォロー中の更新、未読・既読、更新日順
- 作品詳細：作品情報、作者、掲載サイト、話一覧、フォロー、Amazonリンク
- 作者詳細：作者の作品とフォロー
- サイト詳細：掲載作品とフォロー
- マイページ：匿名利用者のフォロー一覧、未読一覧、既読管理
- 固定ページ：利用規約、プライバシーポリシー、アフィリエイト開示

## 9. テスト方針

- 取得アダプターの単体テスト：固定HTML／RSSを使い、話数、タイトル、日時、URL、除外対象を検証する。
- 差分検出の単体テスト：新規、重複、URL変更、取得失敗を検証する。
- DB/APIの統合テスト：フォロー、既読化、未読戻し、匿名ID分離を検証する。
- 画面のE2Eテスト：検索、フォロー、更新確認、公式リンク遷移を検証する。
- 定期処理の動作確認：2サイトの取得、エラー記録、再実行を確認する。

## 10. 運用・セキュリティ境界

### 常に行う

- サイトごとの取得頻度を制限する
- 外部HTMLを信頼せず、取得値を検証・正規化する
- CookieにSecure、HttpOnly、SameSite等の適切な属性を設定する
- 漫画本文・画像を保存しない
- 取得エラーと最終成功日時を記録する
- 前回話数、今回話数、連続失敗回数を記録する
- HTTP 200でも話数0件などの異常を検知する
- 規約・プライバシー・アフィリエイト開示を公開する

### 事前に確認する

- 対応サイトの利用規約・robots.txt
- Amazonアソシエイト申請とリンク表示条件
- 独自ドメインの取得
- Supabase FreeからProへの移行
- 新しい対応サイトの追加
- HTML解析処理の変更

### 行わない

- ユーザーが指定した任意サイトの無制限クロール
- ログインが必要なページの取得
- 漫画本文・画像の転載
- Amazonの商品情報の無許可な長期キャッシュ
- 秘密情報、パスワード、APIキーのリポジトリ保存

## 11. 開発コマンド

プロジェクト作成後に確定する。想定コマンドは以下のとおり。

```text
開発：npm run dev
ビルド：npm run build
テスト：npm test
Lint：npm run lint
型チェック：npx tsc --noEmit
```

## 12. 未決定事項

- 公開用の独自ドメイン名
- MVP後に追加する対応サイト
- Amazonアソシエイト承認後の商品画像表示の可否
- HTML解析がJavaScript実行を必要とするサイトへの対応方針

## 13. サービス名称

- サービス名：WEBマンガウォッチ
- サブタイトル：読んでいるWEBマンガの更新を、ひとまとめに。
